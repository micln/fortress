# Fortress War

一个使用 Godot 4.6 开发的竖屏城堡攻防原型项目，目标平台为手机，交互以触控为主，同时兼容鼠标。

## 当前能力

- 随机生成由城市和道路组成的地图
- 城市会从古代中国城市候选池中随机命名
- 城市采用更偏古代中国城池的符号化图标，而不是简单圆点
- 每座城市都有等级，不同等级对应不同的人数上限
- 被占领城市每秒产出 1 名士兵，达到人数上限后停止产兵，中立城市不产兵
- 玩家和多个独立电脑势力都可以从一个城市向相邻城市发起进攻
- 己方城市到己方城市会执行运兵，且可跨越整张地图直接输送，不受道路相邻限制
- 玩家下达命令时会弹出出兵数量对话框，可手动选择派多少人
- 打开出兵数量对话框时，战局会自动暂停，确认或取消后恢复
- 对局中可随时手动暂停，再继续当前战局，暂停不会弹出遮挡战场的大面板
- 开局可选择总方数，默认 4 方，即 1 个玩家加 3 个独立电脑势力
- 电脑 AI 提供难度和风格选项，难度会影响出手频率与出兵冗余，风格会影响进攻倾向
- 进攻到达时只要攻方人数大于等于守军，就会占领目标城市
- 出兵不会瞬间完成，地图上会出现实际行军单位，行军时间与城市距离相关
- 不同势力的行军单位若在道路上相遇，会先战斗抵消，剩余部队再继续前进
- 战斗会在行军单位抵达目标城市时才结算
- 同一帧多支队伍同时到达时，先结算进攻，再结算运兵
- 提供程序生成的背景音乐与操作音效
- 提供草地与土地风格的程序背景
- 提供一个最小可玩的竖屏原型与核心规则测试

## 目录结构

- `scenes/`：场景文件
- `scripts/domain/`：领域规则与实体
- `scripts/application/`：流程编排、地图生成、AI 决策
- `scripts/presentation/`：界面渲染、输入交互、HUD
- `tests/`：核心规则测试
- `docs/`：设计与约束文档

## 运行方式

1. 使用 Godot 4.6 打开项目根目录
2. 运行主场景 `scenes/main/prototype_main.tscn`
3. 阅读开局说明后点击“开始游戏”
4. 点击蓝色己方城市，再点击目标城市打开出兵面板
5. 点击敌方或中立城市时，只有相邻且有道路连接才可进攻
6. 点击己方城市时，无论是否相邻都可直接运兵
7. 城市会显示等级和当前人数上限，例如 `长安 Lv.2` 与 `18/35`
8. 开始游戏前可在说明面板中选择总方数、难度与风格
9. 出兵面板打开时，行军、产兵和电脑 AI 会自动暂停
10. 对局中可用底部“暂停”按钮冻结当前战局，再点击继续恢复
11. 选中后不会自动取消，也可以用底部“取消选择”按钮慢慢操作
12. 想重新来一局时，使用底部“重新开始”或结束面板按钮

## 测试方式

可通过 Godot headless 模式执行 `tests/test_runner.gd` 来验证当前原型核心规则：

```bash
HOME=/tmp XDG_DATA_HOME=/tmp godot --headless --path . -s res://tests/test_runner.gd
```
