# Implementation Notes

## 代码组织

- `domain` 只处理纯规则与数据状态，不依赖场景树
- `application` 负责编排时间推进、地图初始化、敌方行为
- `presentation` 负责输入、绘制、HUD 和与场景树交互

## 交互约定

- 手机优先，单指点击完成选城和出兵
- 鼠标左键行为与触控保持一致
- 主界面保持竖屏安全区域意识，信息布局优先顶部与底部

## 实现约束

- 优先使用可测试的纯逻辑类承载核心结算规则
- 场景层仅做状态展示与转发输入，不直接写战斗规则
- 任何新增玩法都要同步更新 `docs/` 中对应文档
